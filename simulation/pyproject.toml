[project]
name = "geodesics-of-care-sim"
version = "0.1.0"
description = "Illustrative Fisher-Rao geometry, assigned effort, entropy-constrained utility and recipient permission"
requires-python = ">=3.10"
dependencies = [
    "numpy>=1.26",
    "matplotlib>=3.8",
]
